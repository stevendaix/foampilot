# Audit OF13 — solidDisplacement/square

Le chemin de référence `/opt/openfoam13/tutorials/solidDisplacement/square` n’existe pas dans l’installation OpenFOAM 13 utilisée pour cette intégration. L’énumération de `/opt/openfoam13/tutorials/solidDisplacement` ne contient que `beamEndLoad` et `plateHole`.

Conformément à la règle de comparaison sur la même version OpenFOAM, aucun runner FoamPilot n’est créé pour ce nom et aucune validation ne peut être lancée. La matrice conserve l’entrée afin de tracer l’écart entre la liste de suivi et les tutoriels réellement distribués par OF13.

Statut : **absent de la distribution OpenFOAM 13 — non applicable**.
