FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      topoSetDict;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

actions
(
    {
        name    brick;
        type    cellSet;
        action  new;
        source  boxToCell;
        sourceInfo
        {
            box (59.9 100.0 0.0)(60.0 150.0 10.0);
        }
    }
    {
        name    brick;
        type    cellZoneSet;
        action  new;
        source  setToCellZone;
        sourceInfo
        {
            set    brick;
        }
    }
);

// ************************************************************************* //
